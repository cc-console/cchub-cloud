{
  "name": "cc-console-app",
  "private": true,
  "version": "0.2.19",
  "description": "cc-console desktop shell (Tauri) — wraps the daemon as a native app",
  "scripts": {
    "sidecar": "sh scripts/prepare-sidecar.sh",
    "tauri": "tauri",
    "dev": "npm run sidecar && tauri dev",
    "build": "npm run sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
